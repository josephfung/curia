{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "skills-config",
  "description": "Schema for config/skills.yaml — MCP server connection configuration.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "servers": {
      "type": "array",
      "description": "List of MCP servers to connect to at startup.",
      "items": {
        "$ref": "#/$defs/server"
      }
    }
  },
  "$defs": {
    "actionRisk": {
      "description": "Minimum autonomy score required to invoke tools from this server without explicit CEO approval.",
      "oneOf": [
        {
          "type": "string",
          "enum": ["none", "low", "medium", "high", "critical"]
        },
        {
          "type": "integer",
          "minimum": 0,
          "maximum": 100
        }
      ]
    },
    "server": {
      "type": "object",
      "description": "An MCP server to connect to at startup. All tools discovered via tools/list are registered in the skill registry.",
      "required": ["name", "transport", "action_risk"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Unique identifier for this MCP server. Used in log messages and as a namespace prefix for discovered tool names."
        },
        "transport": {
          "type": "string",
          "enum": ["stdio", "sse"],
          "description": "Transport type. 'stdio' spawns a local process; 'sse' connects to an HTTP MCP endpoint (uses StreamableHTTPClientTransport)."
        },
        "action_risk": {
          "$ref": "#/$defs/actionRisk",
          "description": "Required. Action risk applied to all tools discovered from this server. No default — must be declared explicitly."
        },
        "sensitivity": {
          "type": "string",
          "enum": ["normal", "elevated"],
          "description": "Sensitivity level for all tools from this server. Defaults to 'normal'."
        },
        "timeout_ms": {
          "type": "integer",
          "minimum": 1,
          "description": "Per-invocation timeout in milliseconds for tools from this server. Defaults to 30000."
        },
        "command": {
          "type": "string",
          "description": "Required for stdio transport. The executable to spawn (e.g. 'npx', 'node', '/usr/bin/python')."
        },
        "args": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Command-line arguments for the spawned process. Used with stdio transport only."
        },
        "env": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "description": "Extra environment variables to set in the spawned process. Used with stdio transport only."
        },
        "headers": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "description": "HTTP headers sent with every request to the MCP endpoint. Used with sse transport only. Useful for Authorization: Bearer <token>. Note: values are literal strings — no env-var interpolation is performed."
        },
        "url": {
          "type": "string",
          "description": "Required for sse transport. The HTTP MCP endpoint URL (e.g. https://bigquery.googleapis.com/mcp)."
        },
        "fixed_inputs": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "description": "Constant parameter values injected into every tool call from this server. Keys are parameter names; values are literal strings or 'env:VAR_NAME' references resolved at startup. Listed parameters are stripped from the tool schema — agents never see them."
        },
        "secrets": {
          "type": "array",
          "description": "Declared secrets for this MCP server. Each entry names a flat vault key, provides a console label, and specifies how the resolved value is injected into the subprocess (via env var or fixed tool input). Replaces the old env:\"\" sentinel and fixed_inputs:\"env:VAR\" patterns.",
          "items": {
            "$ref": "#/$defs/secretDeclaration"
          }
        }
      }
    },
    "secretDeclaration": {
      "type": "object",
      "description": "A single secret declared by an MCP server entry.",
      "required": ["key", "label", "required", "secret", "inject"],
      "additionalProperties": false,
      "properties": {
        "key": {
          "type": "string",
          "minLength": 1,
          "description": "Flat vault key (shared across skills and MCP servers). E.g. 'google_oauth_client_id'."
        },
        "label": {
          "type": "string",
          "minLength": 1,
          "description": "Human-readable label shown in the console credential form."
        },
        "required": {
          "type": "boolean",
          "description": "If true, the server cannot be enabled until this secret is configured in the vault."
        },
        "secret": {
          "type": "boolean",
          "description": "If true, the console renders the field as a masked (password) input."
        },
        "inject": {
          "type": "object",
          "description": "How the resolved vault value is delivered to the subprocess. Exactly one of 'env' or 'fixed_input' must be set.",
          "additionalProperties": false,
          "minProperties": 1,
          "maxProperties": 1,
          "properties": {
            "env": {
              "type": "string",
              "minLength": 1,
              "description": "Inject as an environment variable with this name."
            },
            "fixed_input": {
              "type": "string",
              "minLength": 1,
              "description": "Inject as a fixed tool-call input parameter with this name."
            }
          }
        }
      }
    }
  }
}
