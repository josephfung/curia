{
  "name": "list-learning-digest",
  "description": "List the pending voice-guide proposal and sent-mail task-completion undo/confirm items for the daily CEO digest. Returns structured items plus pre-rendered markdown sections (always present; an empty string when nothing renders).",
  "version": "0.2.1",
  "sensitivity": "normal",
  "action_risk": "none",
  "inputs": {},
  "outputs": {
    "voice_guide": "string | null (pending proposed writing-voice guide markdown, or null)",
    "completion_items": "object[]",
    "sections_markdown": "string (always returned; the non-empty sections joined together, or an empty string when nothing renders)"
  },
  "permissions": [],
  "secrets": [],
  "timeout": 15000,
  "capabilities": ["entityMemory"],
  "allowed_callers": ["coordinator"]
}
