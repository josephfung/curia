{
  "name": "scheduler-report",
  "description": "Report the outcome of a scheduled job run. Writes a summary and optional structured context to the job's run record so the CEO and agents can review what happened on the last execution. When called from a scheduled run, job_id is optional — the runtime derives it from the conversation. Outside a scheduled run, pass job_id explicitly.",
  "version": "1.2.0",
  "sensitivity": "normal",
  "action_risk": "none",
  "inputs": {
    "job_id": "string? (optional inside a scheduled run — derived from conversation; required outside one)",
    "summary": "string",
    "context": "object?"
  },
  "outputs": {
    "success": "boolean"
  },
  "permissions": [],
  "secrets": [],
  "timeout": 15000,
  "capabilities": [
    "schedulerService"
  ]
}
