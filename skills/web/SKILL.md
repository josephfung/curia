---
name: web
description: >
  Web fetch, search (Tavily-gated), and browser tools. Per-tool install.requires_secrets gating is preserved — the bundle does not gate on Tavily.
version: "0.1.0"
tools:
  - web-fetch
  - web-search
  - web-browser
---
